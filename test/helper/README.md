# cqrs-4-java-test-helper

Test doubles: preconfigured KurrentDB and MariaDB containers, a TCP proxy that can cut the connection to one, and `StubAuthServer` for tests that must not start a Keycloak.
