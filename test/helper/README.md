# cqrs-4-java-test-helper

Test doubles: preconfigured KurrentDB, MariaDB and Keycloak containers, a TCP proxy that can cut the connection to one, `StubAuthServer` for tests that must not start a Keycloak, `KeycloakRealm` for those that must, and `SecurityArchRules`.
